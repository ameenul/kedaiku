<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_group_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TextView untuk menampilkan nama group -->
    <TextView
        android:id="@+id/textViewGroupName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nama Group"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_margin="16dp" />

    <!-- RecyclerView untuk menampilkan anggota group -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewGroupMembers"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/textViewGroupName"
        android:layout_marginTop="8dp" />

    <!-- FloatingActionButton untuk menambah anggota -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddMember"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add"
        android:contentDescription="Tambah Anggota"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp" />

</RelativeLayout>
